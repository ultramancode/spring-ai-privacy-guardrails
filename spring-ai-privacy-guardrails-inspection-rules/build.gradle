description = 'Bounded rule-based content inspection.'
dependencies {
    api project(':spring-ai-privacy-guardrails-inspection-core')
    implementation "com.google.re2j:re2j:${providers.gradleProperty('re2jVersion').get()}"
}
